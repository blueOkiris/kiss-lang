name:                kiss-lang
version:             0.1.0.0
github:              "blueOkiris/kiss-lang"
author:              "Dylan Turner"
copyright:           "2020 Dylan Turner"

extra-source-files:
- README.md

description:         Please see the README

dependencies:
- base >= 4.7 && < 5
- directory >= 1.3.6.0

library:
  source-dirs: src

executables:
  smooch:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - kiss-lang
